<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.meusmedicamentos">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.meusmeducamentos.MainActivity"
            android:label="MENU">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.meusmeducamentos.CadPacientes"
            android:label="PACIENTES"/>

        <activity android:name="com.meusmeducamentos.CadMedicamentos"
            android:label="MEDICAMENTOS"/>

        <activity android:name="com.meusmeducamentos.Historico"
            android:label="HISTÓRICO"/>

    </application>

</manifest>